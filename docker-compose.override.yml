services:
  api:
    build:
      context: .
      target: dev
    command: npm run dev
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      NODE_ENV: development
    ports:
      - "3000:3000"

volumes:
  node_modules:

